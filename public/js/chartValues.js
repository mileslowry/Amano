$(document).ready(() => {
    //pool.chemReading[0].pH;
    const alk = pool.chemReading[0].alk;
    const cl = pool.chemReading[0].alk;

    $(document).load(() => {
        const pH = 7;
        console.log(pH)
        setPHReading(pH);
    });
    
});